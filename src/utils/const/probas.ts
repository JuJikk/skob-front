import proba_zero from '../../../public/data/proba_zero.json';
import proba_first from '../../../public/data/proba_first.json';
import proba_second from '../../../public/data/proba_second.json';
import proba_zero_female from '../../../public/data/proba_zero_female.json';
import proba_first_female from '../../../public/data/proba_first_female.json';
import proba_second_female from '../../../public/data/proba_second_female.json';

export const zeroSample = proba_zero;

export const firstSample = proba_first;

export const secondSample = proba_second;

export const zeroSampleFemale = proba_zero_female;

export const firstSampleFemale = proba_first_female;

export const secondSampleFemale = proba_second_female;
